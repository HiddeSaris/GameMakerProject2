output_dir = real(_dir);
image_speed = 0
image_index = 0//obj_manager.ds_hydration_index[# grid_x, grid_y] + 1;
//image_index = irandom(1)

grid_x = pos_to_grid_x(x, y);
grid_y = pos_to_grid_y(x, y);

function get_data(){
	return {};
}